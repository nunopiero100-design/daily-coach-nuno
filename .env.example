# Daily Coach / Intervals
INTERVALS_API_KEY=
ATHLETE_ID=

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini

# Daily Coach behavior
AUTO_APPLY=false
WEEKLY_AUTO_APPLY=false

# Local storage
DATA_DIR=data

# API auth
APP_TOKEN=change-me-local-token

# SMTP fallback email
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
TO_EMAIL=
FROM_EMAIL=